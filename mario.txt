#include <iostream>
#include <iomanip>
#include <conio.h>
using namespace std;
int main()
{
  //Початок
 setlocale(0, "");
 cout << "< Строки : ";
 
 char r;
 char p;
 int s;
  cin >> s;
  printf("\n");
 cout << "<1 - Перший варiант> \n";
 cout << "<2 - Другий варiант> \n";
  cin >> p;
//Робочий код
if (p == '1')
 {
	      
  for (int i = 0; i < s; i++)
  {
   cout << setw(s - i) << ' ';
   for (int j = 0; j <= i; j++)
    cout << "*";
   cout << endl;

  }

  cout << " " << endl;

 }
 else if (p == '2')
 {
  
  cout << endl;
  cout << "Введите количество строк : ";


  cout << endl;

  for (int i = 0; i < s; i++)
  {
   cout << setw(s - i) << ' ';
   for (int j = 0; j <= i; j++)

    cout << "*";
   cout << " ";

   for (int j = 0; j <= i; j++)
    cout << "*";
   cout << endl;

  }
  cout << " " << endl;
 }
 else if (p == '3')
 {
  exit(0);
 }
 else if (p == 0)
 {
  cout << "Ошибка!" << endl;
  system("pause");
  exit(0);
 }
 //Повтор змінних
 cout << "<Продолжить (y/n) ? >";
 cout << " \n";
 cin >> r;

 if (r == 'y')
 {
  return main();
  cout << " ";
 }
 else if (r == 'n')
 {
  exit(0);
 }
 else
 {
  cout << "Ошибка!" << endl;
  system("pause");
  exit(0);
 }

 cout << endl;

 system("pause");
 return 0;
}